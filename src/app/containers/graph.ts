import { Component } from '@angular/core';
import { LineGraph } from '../ui/index';

@Component({
  selector: 'graph',
  directives: [
    LineGraph
  ],
  styles: [],
  template: `

  `
})
export class Graph {}